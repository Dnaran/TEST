<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.myapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tital"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SlideActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LogoActivity" />
        <activity android:name=".ListViewStingActivity" />
        <activity android:name=".Slider_Activity" />
        <activity android:name=".Register_Activity" />
        <activity android:name=".Collect_Ativity" />
        <activity android:name=".Strategy_Activity" />
        <activity android:name=".Scene_Activity"></activity>
    </application>

</manifest>